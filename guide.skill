Media Manager - 数据同步与媒体查看体验重构规范
一、数据同步系统重构
设计原则
APP不存在“缓存”概念。
系统只存在以下三种数据状态：
本地数据（Local Data）
云端数据（Cloud Data）
数据同步（Sync）
参考思源笔记的数据管理模式。
禁止出现：
清理缓存
缓存大小
缓存管理
缓存路径
等设计。
统一改为：
本地数据管理
云端数据管理
数据同步管理
数据快照管理

同步账号逻辑
首次启用同步时必须配置：
WebDAV地址
用户名
密码
未登录时：
主页同步按钮显示：
未配置同步
点击进入同步配置页面。
同步配置完成后：
自动建立同步关系。

首页同步入口设计
位置：
主页顶部工具栏右侧。
样式：
圆角矩形按钮。
显示内容：
同步
右下角显示同步状态图标。

同步状态
未同步
图标：
灰色云朵
状态：
未同步

同步中
图标：
旋转箭头
动画：
持续旋转
状态：
同步中
点击后显示：
正在同步

上传：
IMG_20260701.jpg

已完成：
12/153
实时刷新。

同步成功
图标：
绿色对勾
状态：
同步成功
点击显示：
最近同步

2026-08-04 18:20:35

上传:
25个文件

下载:
4个文件

同步失败
图标：
红色叉号
状态：
同步失败
点击显示：
失败原因

WebDAV连接失败

服务器返回:
401 Unauthorized

数据快照系统
参考思源笔记。
支持：
自动快照
手动快照
快照恢复
显示：
今日快照
昨日快照
本周快照
恢复前必须确认。

二、动态页媒体封面修复
视频封面
当前问题：
动态页面：
灰色横条
视频图标
播放按钮
无封面。
详情页面：
黑色区域
播放按钮
全屏按钮
无封面。

修复要求
上传视频后自动生成封面。
生成规则：
默认取第1秒关键帧
失败时：
取首帧

动态页面
显示：
视频封面
播放图标
时长标签
布局：
16:9圆角矩形卡片。

详情页面
显示：
视频封面
播放按钮
时长
全屏按钮
进入页面时：
先显示封面。
点击播放：
切换播放器。
禁止默认黑屏。

三、图片显示修复
当前问题
上传图片后：
动态页：
仅显示图片图标。
详情页：
仅显示图片图标。
无法预览。

修复要求
上传完成后：
立即生成缩略图。
动态页：
显示缩略图。
详情页：
显示原图预览。
支持：
JPG
PNG
WEBP
GIF
HEIC

四、统一设计语言
设计原则
全APP统一：
圆角矩形设计。
禁止：
直角卡片
直角按钮
直角弹窗

圆角规范
卡片：
16dp
按钮：
14dp
弹窗：
20dp
输入框：
14dp
媒体缩略图：
16dp

五、图片查看器重设计
进入方式
点击图片进入全屏查看器。

页面结构
顶部：
返回按钮
图片信息按钮
更多按钮

中间：
图片区域
黑色背景
支持：
双击缩放
双指缩放
拖动查看
原图加载

底部：
图片数量指示器
例如：
3 / 12

功能菜单
支持：
保存到相册
分享
添加标签
查看EXIF
删除

六、视频查看器重设计
设计目标
视频查看器与图片查看器风格一致。

页面结构
顶部：
返回按钮
视频信息按钮
更多按钮

中间：
视频区域
保持原比例显示。
禁止自动横屏。
禁止强制旋转设备。

播放模式
进入页面：
显示封面。
点击播放：
开始播放。
暂停：
恢复显示播放按钮。

全屏逻辑
全屏：
仅表示占据整个应用窗口。
不是自动横屏。
不是旋转设备。
保持当前方向。

横屏按钮
播放器内部提供：
横屏播放
按钮。
用户主动点击后：
进入横屏模式。
退出恢复原方向。

底部控制栏
显示：
播放/暂停
进度条
当前时间
总时长
音量
横屏按钮

七、标签系统入口统一
图片和视频统一支持：
添加标签
编辑标签
查看标签
入口位置：
详情页面右上角菜单。
支持批量管理。