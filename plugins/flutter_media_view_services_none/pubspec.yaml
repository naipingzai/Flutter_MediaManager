name: flutter_media_view_services_platform
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_media_view_map:
    path: ../flutter_media_view_map
  flutter_media_view_services:
    path: ../flutter_media_view_services
  latlong2:

dev_dependencies:
  flutter_lints:

flutter:
