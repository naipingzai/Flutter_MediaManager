name: fmv_video
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  fmv_model:
    path: ../fmv_model
  fmv_utils:
    path: ../fmv_utils
  leak_tracker:

dev_dependencies:
  flutter_lints:

flutter:
