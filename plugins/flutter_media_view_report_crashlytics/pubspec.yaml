name: flutter_media_view_report_platform
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_media_view_report:
    path: ../flutter_media_view_report
  firebase_core:
  firebase_crashlytics:
  stack_trace:

dev_dependencies:
  flutter_lints:

flutter:
